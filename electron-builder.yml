# Packaging config for `npm run dist` (Windows NSIS installer, per-user, no admin needed). See README > Building and releasing.
appId: dev.vivek.ora
productName: Ora
directories:
  output: dist
files:
  - out/**
  - package.json
asar: true
win:
  icon: build/icon.ico
  target: nsis
  artifactName: Ora-Setup-${version}.${ext}
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
